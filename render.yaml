services:
  - type: web
    name: flask-ml-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn application:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
